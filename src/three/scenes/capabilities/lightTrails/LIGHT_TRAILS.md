# Light trails

Reference: [Artisans d’idées](https://www.artisansdidees.com/en), inspected September 8, 2026. Motion (He, qe), line camera and MeshLine geometry were read from the public acefdce.js bundle and checked against the running reference.

ReferenceTrailMotion uses seven free-base FABRIK chains with 41 bones of length 8, mouse interpolation 0.02–0.10 per strand, sine/cosine speed 1.44 and amplitudes 1.69/3.18, and the original seeded simplex permutations. Depth moves between ±20–50 in three-second tweens. Click spins use the original local target orbit, duration and 0.07-second stagger. Repeated clicks remain guarded by the existing scene interaction.

The critical flight component is 160 reference units per second (20 × rig speed 8). We keep camera-relative coordinates: move existing points forward before solving each new target. This lets an old sideways bend pass the viewer and restores the long forward shape while the cursor stays still. Never remove this flight or replace it with a stationary head and curling chain. A fixed 60 Hz simulation preserves the reference interpolation across render rates. Render coordinates interpolate between the last two solver states on every display frame, including 120/144/165 Hz and uneven frame times; the solver and camera response stay unchanged. Two reusable coordinate buffers avoid runtime allocation. Chains settle once under prepare, before reveal.

Rendering follows the original 40-sample MeshLine, texture coordinates, projected previous/next tangents, constant 0.56 world-unit width and 80-degree field of view. There is no extra Catmull interpolation, synthetic taper, constant screen-width compensation or duplicate glow ribbon. Coordinates and width are uniformly scaled by 0.095 into our scene. Preserve the reference offset (-45, -25, -156), which puts the bundle behind the lower-left edge and its heads below/left of center. Camera yaw/pitch use 36 degrees with reference damping; translation, velocity roll and seeded idle roll follow the reference. HDR radiance adapts our blue/white colors to the existing site bloom threshold. The environment and compositor retain their current owners.

Desktop idle/leave retains the last cursor target; flight washes out the old bend. No timer recenters the camera. Touch release eases line targets back over one second, while the camera retains its last owned target. Passive line/camera motion uses SceneManager's visualPointer over the whole viewport, including the left menu and other HTML chrome. Canvas-to-HTML pointerleave must not count as leaving the viewport; the window capture listener also restores tracking when re-entering over a menu. Click spins still require the owned interaction pointer and its Y-band, so menu clicks do not trigger them. Actual leave/blur retains the last target.

Geometry, materials, points and the 42×7 DataTexture are prepared once and reused. Runtime only updates coordinates, transforms and uniforms. Dormant scenes stop simulation. No new renderer, animation loop, CanvasTexture or runtime geometry rebuild.

Validation: node --test src/three/scenes/capabilities/lightTrails/lightTrailsMotion.test.js covers flight/bend recovery, bone lengths, noise samples measured from the reference, perspective/framing, 30/60/120 fps equivalence, smooth 120/144/165 Hz and uneven render updates, camera retention, touch, passive tracking over menus, click ownership and resource reuse. Browser checks compare original and local idle/cursor views, including a render with captured original coordinates. High/medium/low smoke checks verify linked programs and stable GPU resources. These scene-level checks do not assert full-site transition performance.

## Triangular passage

The passage keeps the user's triangular-tunnel direction with illuminated structural ribs and dark graphite metal. Cladding uses thin, nearly flush sheets instead of thick rounded blocks. Large plain sections establish the structure; smaller service doors, ventilation sections and sparse catches provide detail. The approved foreground trails, camera response, retained target, flight speed and living shoals stay intact.

Three walls surround a 36-unit inscribed radius, with 128 longitudinal bays and 18 lanes per side. Two of every three lanes use sheets spanning two bays; the other lane uses shorter service sections. There are 4,608 sheets and 576 nearly flush service catches. Thirty-two complete triangular frames repeat every 48 units through the 1,536-unit cycle. Each rib carries four interrupted light inlays and three structural collars. The entire architecture uses 5,952 instances of one prepared module, 238,080 triangles and one draw (about 40% fewer triangles than the previous cladding).

The module has a flat face, narrow bevel and straight sides. Coincident vertices split the face/bevel/side normals so interpolation cannot inflate the face. Sheets are only 0.30–0.36 units thick, with narrow consistent joints. Shader detail adds inset door seams, louvers, captive screws, etched IDs and filtered brushed-metal roughness. GGX direct specular, Fresnel and broad ambient illumination distinguish graphite faces from exposed cut edges. Repeating analytic rib reflections were removed because their blue bands confused the physical tunnel structure; do not reintroduce copies of the frames on the cladding. No reflection texture, extra renderer, ray-tracing pass or environment capture is allocated. Fine detail fades with distance and compiles out on low quality. Approximately 6.5% of sheets gently extend by 0.16–0.36 units and return on independent seeded cycles; catches share their motion. Geometry, matrices and seeds remain immutable after preparation.

Depth is scene-local infinite-far with a 0.5-unit near plane shared by the architecture and particles. This improves depth precision for distant recessed light strips without changing the camera projection or other scenes. Geometry fades before the 1,536-unit wrap; no new segment is generated during flight. The existing readyPromise, warm gate, dormant gate and disposal owner remain in charge.

### Narrative and sound

Six prepared two-line captions alternate between the walls after a 1.5-second opening delay. Their upright world-space pose stays independent of the camera, inset from the ribs, with a separate slow forward drift. The architecture retains its 26-unit-per-second flight. Caption timing, placement and localized copy live in `../typography`; glyph pixels are painted once under prepare, while reveal and motion use uniforms and transforms.

Appearing and disappearing mosaic cells briefly shift and emit cyan HDR radiance into the existing site bloom. There is no local halo or additional bloom pipeline. Core typography keeps its sharp copy after bloom, with an emission-only prepared mesh in the models layer; both share the atlas, geometry and reversible playhead. That emission disappears at rest, while settled text uses the original sharp one-sample shader path. Warmup draws a partial reveal so the emission participates in the prepared RT draw.

The text sound uses the home page Digital Monster title's original `logo_reveal` + `glitch_button` pair at their recorded relative levels. Both cached assets are summed once under the curtain into their actual maximum duration (about 0.72 s), without silent padding. Both directions use this forward recording without granular remix, reversal or EQ. Gain 0.8 and a 12 ms gain time constant keep large titles present over the bed. Each title retains its painted line widths during atlas preparation; `getSoundReveal()` maps the shader's 0.72 sweep, 0.1 stagger and 0.18 cell phase onto the occupied text interval, accounting for locale, state, side and the city's compact layout. Audio maps the complete recording onto that visual interval, corrects small clock drift through playback rate, and only seeks on reversal or drift over 120 ms. A 25 ms fade stops settled/paused titles. Route visibility controls volume only, so hex motion cannot trigger a false mosaic exit. The flight and line-motion mix remains independent.

`CapabilitySceneSound` follows painted text reveal and the motion of the seven rendered line heads. The line buffer uses irregularly spaced sine components centred near 520 Hz with a quieter 1160 Hz layer. Unequal spacing avoids the old regular beating pattern. Movement gain is capped at 0.13, playback rate at 0.96–1.12 and low-pass cutoff at 1150–1500 Hz; 160 ms exponential smoothing rounds the response to fast bends. The gain curve uses a 0.05 motion threshold and exponent 1.2 so small bends remain audible without raising the maximum. The sixteen-second buffer closes periodically and is prepared once; its playhead continues across quiet gaps instead of restarting the same opening. The air bed uses gain 0.045, modestly restored from 0.095 / 3 while remaining over four times below the original 0.19 level. Both prepared loops share the site audio bus, mute/visibility handling and a renewable fade-stop deadline so dormant scenes cannot leave a loop playing.

### Depth particles

Particle budgets remain 800 / 1,600 / 2,600 for low / medium / high, including one fixed distant glow point. Within those budgets, 8 / 14 / 24 seeded shoals contain 3 / 5 / 7 luminous heads each, with 4 / 5 / 5 prepared tail samples per head. Shoal centres drift, members orbit and gently flicker, and tails sample earlier path positions. Swarm radii remain inside the shell; speed differs from the dust. Their depth expression preserves continuity when travel wraps. Remaining points are dust; the fixed glow stays at the vanishing region. All are prepared together, with no CPU spawning, extra draw call or position-buffer uploads during flight.

Particles share the shell's infinite-far depth mapping, test against its geometry and do not write depth. Perspective/subpixel coverage and near/far fading limit visual density. Pixel scale follows the active render target or drawing buffer through the existing onBeforeRender uniform update.

Validation covers the original motion/pointer contracts, bounded topology, clear flight volume and bounded panel sizes, full-cycle resource reuse, quality budgets, resize and disposal. Browser checks cover high/low rendering, side views, a full flight loop, context restoration and leaving/returning through the site's existing route lifecycle.
