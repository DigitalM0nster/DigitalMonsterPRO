# Archived case pages

These React route shells are the previous `/portfolio/:caseId` implementation.
They are intentionally disconnected from the active portfolio route while the new
in-hub case experience is being built.

Project data, prepared Three.js scenes, shared case HUD infrastructure, and runtime
resources remain in their active folders because the preloader and transition
pipeline still depend on them.
