/** Референс: искажение HTML-текста (overlay + Three.js). */
export const DOM_DISTORT_DEMO_PATH = "/demo/distort";

export function isDomDistortDemoPath(pathname) {
	return pathname === DOM_DISTORT_DEMO_PATH;
}
